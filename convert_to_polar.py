import math
from coordinates import coordinates

if __name__ == "__main__":
    polar = {}
    for i in xrange(2, 77):
        angle = 0
        if coordinates[i]["x"] - coordinates[i]["y"] == 0:
            if coordinates[i]["y"] > 0:
                angle = math.
            else:
        math.tan()

